{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm install -g pnpm@9 && pnpm install --frozen-lockfile=false && pnpm --filter @workspace/nordic-stangsel run build",
  "outputDirectory": "artifacts/nordic-stangsel/dist",
  "installCommand": "echo 'Skipping default install; handled in buildCommand'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
